<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace值 = Mapper所对应接口的全限定符-->
<mapper namespace="org.example.mapper.EmployeeMapper">

<!--  声明： 标签写SQL语句 crud select insert update delete
        每个标签对应接口的一个方法，方法的一个实现！
        注意： 以后的Mapper接口 不能重载， 因为Mapper xml文件无法识别
-->
    <select id="queryById" resultType="org.example.pojo.Employee">
        <!-- #{empId}代表动态传入的参数,并且进行赋值!后面详细讲解 -->
        select emp_id empId,emp_name empName, emp_salary empSalary from
        t_emp where emp_id = #{id}
    </select>

    <delete id="deleteById">
        delete from t_emp where emp_id = #{id}
    </delete>




    <!--xml方式写SQL语句的， mybatis会规定在特定的标签内 写 SQL语句 -->

    <!-- 查询使用 select标签
            id = 方法名
            resultType = 返回值类型
            标签内编写SQL语句
     -->

</mapper>